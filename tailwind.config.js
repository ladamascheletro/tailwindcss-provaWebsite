const colors = require('tailwindcss/colors')

module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    colors: {
      'primary': '#f8f0ed',
      'secondary': '#222222',
    },
    extend: {}, 
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
